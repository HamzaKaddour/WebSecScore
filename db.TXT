CREATE TABLE `websecscore`.`scan_domains` (
  `ScanID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `Domain` VARCHAR(45) NOT NULL,
  `Score` FLOAT NOT NULL,
  `HIBP` JSON NULL,
  `MALWARE` JSON NULL,
  `OPEN_PORTS` JSON NULL,
  `THREATFOX` JSON NULL,
  `VIRUSTOTAL` JSON NULL,
  `WAPITI` JSON NULL,
  `CMS` JSON NULL,
  `VISA` JSON NULL,
  `CSA` JSON NULL,
  PRIMARY KEY (`ScanID`),
  UNIQUE INDEX `ScanID_UNIQUE` (`ScanID` ASC) VISIBLE);
